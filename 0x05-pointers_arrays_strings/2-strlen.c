#include "main.h"
#include <string.h>
/**
 * _strlen - Calculates the length of a string
 * @s: The string to evaluate
 * Return: The length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
